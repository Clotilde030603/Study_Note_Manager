{
  "name": "study-note-manager-backend",
  "version": "1.0.0",
  "description": "Express REST API server for Study Note Manager",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/db/pool.js && node --check src/controllers/noteController.js && node --check src/routes/noteRoutes.js && node --check src/routes/healthRoutes.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mysql2": "^3.11.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
